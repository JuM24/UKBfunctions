% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc_stats.R
\name{desc_stats}
\alias{desc_stats}
\title{Return table of descriptive statistics.}
\usage{
desc_stats(
  df,
  variables = NULL,
  central_tendency = "mean",
  stratify_by = NULL,
  useNA = TRUE,
  NA_stratify = FALSE,
  sig_fig = 3,
  write = TRUE,
  out_path = NULL
)
}
\arguments{
\item{df}{The input data frame.}

\item{variables}{An array of column names, indicating the variables for which
descriptive statistics should be generated. If \code{NULL}, all variables in the
data frame are used.}

\item{central_tendency}{Character indicating whether the mean and
standard deviation (\code{'mean'}) or the median and interquartile range (\code{'median'})
should be presented. Alternatively, an array of column names, indicating the
variables for which the median/IQR should be presented; for all others,
the mean/SD will be presented.}

\item{stratify_by}{An array of column names, indicating the variables to use
to stratify the presentation of the statistics. E.g., if
\code{stratify_by = c('var_a')}, separate columns will be created for different
levels of 'var_a'. If \code{NULL}, no stratification occurs.}

\item{useNA}{Logical; whether the numbers of missing values should be presented as
a separate column.}

\item{NA_stratify}{Logical; applies only when \code{useNA = TRUE} and indicates
whether the numbers of missing values should be presented separately for
each level of the variable that are used for stratification as indicated by
the argument \code{stratify_by}.}

\item{sig_fig}{Integer; how many significant figures the results should be
rounded to.}

\item{write}{Logical; whether a .csv file should be written to disk. If so,
the presentation is reformatted. See \code{details} for more.}

\item{out_path}{Character; file path for the .csv output. Required when
\code{write = TRUE}.}
}
\value{
A list with two data frames: \code{raw} containing separate columns for
each metric, and \code{formatted} containing combined presentation-ready columns.
}
\description{
\code{desc_stats} takes a data frame as an input and returns a table with
descriptive statistics for variables in the data frame. For variables of
class \code{numeric} and \code{integer}, the mean or median and the standard deviation
or interquartile range is returned. For variables of class \code{factor}
and \code{character}, absolute numbers and proportions are returned. Each metric
is returned as a separate column.
}
\details{
When \code{write = TRUE}, proportions are transformed to percentages (\%), and
the mean/median and SD/IQR columns (for \code{numeric} and \code{integer} variables)
and the number and proportion columns (for \code{factor} and \code{character} variables)
are combined into a single column, with a space (' ') separating the two values
and the second value placed in parentheses. E.g., if the R file has two
separate columns with mean and SD '22.5' and '2.91', respectively, the
written table will contain a single column with the entry '22.5 (2.91)'. As
another example, if the R file has two separate columns with n and proportion
'12498' and '0.873', the written table will contain a single column with the
entry '12498 (87.3\%)'. Note that in both above examples, \code{sig_fig = 3}.

When \code{useNA = TRUE}, missing values are presented in the same format as
categorical levels: the \code{raw} table includes both \code{n_missing} and
\code{proportion_missing} columns, and the \code{formatted} table combines them into
a single column (e.g., '5 (10\%)'). In this case, the proportions for all
levels of a categorical variable plus the proportion of missing values sum
to 1 (or close to 1 due to rounding).

The function always returns a list with two elements: \code{raw} (separate columns
per metric) and \code{formatted} (combined columns as described above). When
\code{write = TRUE}, the formatted version is additionally written to disk.
}
